---
import AdminLayout from '../../../layouts/AdminLayout.astro';
import { getSupabaseAdmin } from '../../../lib/supabase';

export const prerender = false;

const id = Astro.params.id;
if (!id) return Astro.redirect('/admin/matches');

const admin = getSupabaseAdmin();

const { data: match } = await admin
  .from('matches')
  .select('*, teams(id, name, club_id, clubs(name)), clubs(name)')
  .eq('id', id)
  .maybeSingle();
if (!match) return Astro.redirect('/admin/matches');

const team = (match as any).teams;
const club = team?.clubs ?? (match as any).clubs;

// Match player stats with player metadata
const { data: psRaw } = await admin
  .from('match_player_stats')
  .select(`
    match_id, player_id,
    qb_att, qb_comp, qb_yds, qb_td, qb_int, qb_sack,
    wr_targets, wr_rec, wr_yds, wr_td, wr_xp, wr_pts,
    db_flag_pull, db_sack, db_brkup, db_int,
    players ( first_name, last_name, jersey_number, position )
  `)
  .eq('match_id', id);
const ps = (psRaw ?? []) as any[];

// All players in the match's team (for "add player to stats" dropdown)
const { data: teamPlayers } = await admin
  .from('players')
  .select('id, first_name, last_name, jersey_number, position')
  .eq('team_id', match.team_id)
  .order('jersey_number', { ascending: true, nullsLast: true });
const playersInStats = new Set(ps.map((r) => r.player_id));
const availablePlayers = ((teamPlayers ?? []) as any[]).filter((p) => !playersInStats.has(p.id));

const url = new URL(Astro.request.url);
const saved = url.searchParams.get('saved');
const errorMsg = url.searchParams.get('error');
const SAVED_MSG: Record<string, string> = {
  '1':       '✓ Změny uloženy.',
  stats:     '✓ Statistiky hráče uloženy.',
  added:     '✓ Hráč přidán do statistik.',
  removed:   '✓ Hráč odebrán ze statistik.',
};
---

<AdminLayout title={`vs ${match.opponent} · ${club?.name ?? ''}`} active="matches" pageTitle={`vs ${match.opponent}`} pageSub={`${new Date(match.date).toLocaleDateString('cs-CZ')} · ${club?.name ?? '—'} · ${team?.name ?? '—'} · skóre ${match.our_score}–${match.opp_score}`}>

  {saved && SAVED_MSG[saved] && <div class="mb-6 rounded-lg border border-verde/30 bg-verde/5 px-4 py-3 text-[14px] text-[#1B6E62]">{SAVED_MSG[saved]}</div>}
  {errorMsg && <div class="mb-6 rounded-lg border border-signal/30 bg-signal/5 px-4 py-3 text-[14px] text-signal">{errorMsg}</div>}

  <div class="mb-6 flex flex-wrap items-center gap-2 text-[13px]">
    <a href="/admin/matches" class="rounded-md border border-stone bg-pearl px-3 py-1.5 font-medium text-graphite hover:bg-mist">← Všechny zápasy</a>
    <a href={`/admin/teams/${match.team_id}`} class="rounded-md border border-stone bg-pearl px-3 py-1.5 font-medium text-graphite hover:bg-mist">Tým: {team?.name}</a>
  </div>

  <!-- Match-level edit form -->
  <div class="rounded-xl border border-stone bg-pearl">
    <div class="border-b border-stone px-5 py-4">
      <p class="text-[15px] font-semibold tracking-tight-2 text-ink">Zápasové statistiky</p>
      <p class="text-[12px] text-slate">Upravujete přímo Supabase data — okamžitý efekt v <code class="rounded bg-mist px-1 py-0.5 text-[11px]">/app/stats</code> a Liga leaderboards</p>
    </div>
    <form method="POST" action="/api/super/match/update" class="p-5">
      <input type="hidden" name="match_id" value={match.id} />

      <div class="grid gap-4 sm:grid-cols-3">
        <label class="block">
          <span class="text-[11px] font-semibold uppercase tracking-[0.08em] text-slate">Datum</span>
          <input type="date" name="date" value={match.date} class="mt-1.5 block w-full rounded-md border border-stone bg-pearl px-3 py-2 text-[13px]" />
        </label>
        <label class="block sm:col-span-2">
          <span class="text-[11px] font-semibold uppercase tracking-[0.08em] text-slate">Soupeř</span>
          <input type="text" name="opponent" value={match.opponent} class="mt-1.5 block w-full rounded-md border border-stone bg-pearl px-3 py-2 text-[13px]" />
        </label>
      </div>

      <p class="mt-6 text-[11px] font-semibold uppercase tracking-[0.08em] text-slate">Skóre + drives</p>
      <div class="mt-2 grid gap-3 sm:grid-cols-4">
        {[
          ['our_score', 'Naše skóre'], ['opp_score', 'Soupeřovo skóre'],
          ['off_drives', 'Útočné drives'], ['off_td', 'Útočné TD'],
          ['def_drives', 'Defenzivní drives'], ['def_stops', 'Defenzivní stops'],
          ['rush_yds', 'Rush yds'], ['pass_yds', 'Pass yds'],
          ['total_yds', 'Total yds'],
          ['opp_rush_yds', 'Opp rush'], ['opp_pass_yds', 'Opp pass'], ['opp_total_yds', 'Opp total'],
        ].map(([k, label]) => (
          <label class="block">
            <span class="text-[10px] uppercase tracking-wider text-stone-2">{label}</span>
            <input type="number" min="0" name={k} value={(match as any)[k] ?? 0} class="mt-1 block w-full rounded-md border border-stone bg-pearl px-2 py-1.5 text-[13px] tabular-nums" />
          </label>
        ))}
      </div>

      <p class="mt-6 text-[11px] font-semibold uppercase tracking-[0.08em] text-slate">QB team agg + naše XP</p>
      <div class="mt-2 grid gap-3 sm:grid-cols-4">
        {[
          ['qb_att', 'QB ATT'], ['qb_comp', 'QB COMP'], ['qb_yds', 'QB YDS'],
          ['qb_td', 'QB TD'], ['qb_int', 'QB INT'],
          ['xp1_att', '1XP ATT'], ['xp1_ok', '1XP OK'],
          ['xp2_att', '2XP ATT'], ['xp2_ok', '2XP OK'],
          ['pen_count', 'Fauly count'], ['pen_yds', 'Fauly yds'],
        ].map(([k, label]) => (
          <label class="block">
            <span class="text-[10px] uppercase tracking-wider text-stone-2">{label}</span>
            <input type="number" min="0" name={k} value={(match as any)[k] ?? 0} class="mt-1 block w-full rounded-md border border-stone bg-pearl px-2 py-1.5 text-[13px] tabular-nums" />
          </label>
        ))}
      </div>

      {/* Iter 24: Soupeřovy XP konverze */}
      <p class="mt-6 text-[11px] font-semibold uppercase tracking-[0.08em] text-slate">Soupeřovy XP konverze (defense)</p>
      <div class="mt-2 grid gap-3 sm:grid-cols-4">
        {[
          ['opp_xp1_att', 'Opp 1XP ATT'], ['opp_xp1_ok', 'Opp 1XP OK'],
          ['opp_xp2_att', 'Opp 2XP ATT'], ['opp_xp2_ok', 'Opp 2XP OK'],
        ].map(([k, label]) => (
          <label class="block">
            <span class="text-[10px] uppercase tracking-wider text-stone-2">{label}</span>
            <input type="number" min="0" name={k} value={(match as any)[k] ?? 0} class="mt-1 block w-full rounded-md border border-stone bg-pearl px-2 py-1.5 text-[13px] tabular-nums" />
          </label>
        ))}
      </div>

      <div class="mt-6">
        <button type="submit" class="rounded-md bg-plum px-4 py-2 text-[13px] font-semibold text-white hover:bg-[#3F2B68]">Uložit změny zápasu</button>
      </div>
    </form>
    <!-- Delete form OUTSIDE the update form (no nested forms) -->
    <div class="border-t border-stone bg-mist/40 px-5 py-3">
      <form method="POST" action="/api/super/match/delete" onsubmit="return confirm('Smazat zápas včetně všech player stats, plays, faulů a drives? Nevratné.')">
        <input type="hidden" name="match_id" value={match.id} />
        <button type="submit" class="rounded-md border border-signal/40 bg-signal/5 px-3 py-2 text-[12px] font-semibold text-signal hover:bg-signal hover:text-white">Smazat celý zápas</button>
      </form>
    </div>
  </div>

  <!-- Per-player stats: editable -->
  <div class="mt-8 rounded-xl border border-stone bg-pearl">
    <div class="flex items-center justify-between border-b border-stone px-5 py-4">
      <div>
        <p class="text-[15px] font-semibold tracking-tight-2 text-ink">Per-hráč statistiky</p>
        <p class="text-[12px] text-slate">{ps.length} hráčů v match_player_stats · upravte čísla a klikněte Uložit u řádku</p>
      </div>
    </div>

    <!-- Add player to stats -->
    {availablePlayers.length > 0 && (
      <div class="border-b border-stone bg-plum/5 px-5 py-3">
        <form method="POST" action="/api/super/match/add-player-stat" class="flex flex-wrap items-end gap-2">
          <input type="hidden" name="match_id" value={match.id} />
          <label class="block flex-1 min-w-[260px]">
            <span class="text-[10px] uppercase tracking-wider text-slate">Přidat hráče (z {team?.name} rosteru)</span>
            <select name="player_id" required class="mt-1 block w-full rounded-md border border-stone bg-pearl px-3 py-1.5 text-[13px]">
              <option value="">— vyberte hráče —</option>
              {availablePlayers.map((p: any) => (
                <option value={p.id}>#{p.jersey_number ?? '—'} {p.first_name} {p.last_name ?? ''} ({p.position})</option>
              ))}
            </select>
          </label>
          <button type="submit" class="rounded-md bg-plum px-3 py-1.5 text-[12px] font-semibold text-white hover:bg-[#3F2B68]">+ Přidat do statistik</button>
        </form>
      </div>
    )}

    {ps.length === 0 ? (
      <div class="p-8 text-center text-[13px] text-slate">Žádné per-hráč staty.</div>
    ) : (
      <div class="divide-y divide-stone">
        {ps.map((row: any) => {
          const p = row.players ?? {};
          const fullName = [p.first_name, p.last_name].filter(Boolean).join(' ') || '—';
          return (
            <div id={`player-${row.player_id}`} class="px-5 py-4">
              <!-- Header row OUTSIDE the update form (remove form sits here, can't be nested) -->
              <div class="mb-3 flex flex-wrap items-center gap-3">
                <span class="inline-flex h-8 w-8 items-center justify-center rounded-full bg-ink/5 text-[12px] font-semibold text-ink">#{p.jersey_number ?? '?'}</span>
                <div class="min-w-0 flex-1">
                  <p class="text-[14px] font-semibold text-ink">{fullName}</p>
                  <p class="text-[11px] text-slate">{p.position ?? '—'}</p>
                </div>
                <form method="POST" action="/api/super/match/remove-player-stat" class="inline">
                  <input type="hidden" name="match_id" value={row.match_id} />
                  <input type="hidden" name="player_id" value={row.player_id} />
                  <button type="submit" class="rounded-md border border-stone bg-pearl px-2 py-1 text-[11px] font-medium text-graphite hover:border-signal/40 hover:text-signal" onclick="return confirm('Odebrat staty tohoto hráče z tohoto zápasu?')">Odebrat ze statistik</button>
                </form>
              </div>

              <form method="POST" action="/api/super/match/update-player-stat" class="space-y-3">
                <input type="hidden" name="match_id" value={row.match_id} />
                <input type="hidden" name="player_id" value={row.player_id} />

                <div class="grid gap-2 sm:grid-cols-6">
                  {[
                    ['qb_att', 'QB ATT'], ['qb_comp', 'COMP'], ['qb_yds', 'YDS'],
                    ['qb_td', 'TD'], ['qb_int', 'INT'], ['qb_sack', 'SACK'],
                  ].map(([k, label]) => (
                    <label class="block">
                      <span class="text-[9px] uppercase tracking-wider text-stone-2">{label}</span>
                      <input type="number" min="0" name={k} value={row[k] ?? 0} class="mt-0.5 block w-full rounded-md border border-stone bg-pearl px-2 py-1 text-[12px] tabular-nums" />
                    </label>
                  ))}
                </div>

                <div class="grid gap-2 sm:grid-cols-6">
                  {[
                    ['wr_targets', 'WR TARG'], ['wr_rec', 'REC'], ['wr_yds', 'YDS'],
                    ['wr_td', 'TD'], ['wr_xp', 'XP'], ['wr_pts', 'PTS'],
                  ].map(([k, label]) => (
                    <label class="block">
                      <span class="text-[9px] uppercase tracking-wider text-stone-2">{label}</span>
                      <input type="number" min="0" name={k} value={row[k] ?? 0} class="mt-0.5 block w-full rounded-md border border-stone bg-pearl px-2 py-1 text-[12px] tabular-nums" />
                    </label>
                  ))}
                </div>

                <div class="grid gap-2 sm:grid-cols-4">
                  {[
                    ['db_flag_pull', 'FLAG'], ['db_sack', 'D SACK'],
                    ['db_int', 'INT'], ['db_brkup', 'BRKUP'],
                  ].map(([k, label]) => (
                    <label class="block">
                      <span class="text-[9px] uppercase tracking-wider text-stone-2">{label}</span>
                      <input type="number" min="0" name={k} value={row[k] ?? 0} class="mt-0.5 block w-full rounded-md border border-stone bg-pearl px-2 py-1 text-[12px] tabular-nums" />
                    </label>
                  ))}
                  <div class="flex items-end">
                    <button type="submit" class="w-full rounded-md bg-plum px-3 py-1.5 text-[12px] font-semibold text-white hover:bg-[#3F2B68]">Uložit hráče</button>
                  </div>
                </div>
              </form>
            </div>
          );
        })}
      </div>
    )}
  </div>
</AdminLayout>
